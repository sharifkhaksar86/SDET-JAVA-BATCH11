package com.syntax.class12Homework;

public class HWLargerNum {
	// Create a method that will take 2 parameters as a numbers and prints which
	// number is larger.

	int numbers(int num1, int num2) {
		if (num1 > num2) {
			return num1;
		} else {
			return num2;
		}
	}
}